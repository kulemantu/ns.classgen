# API Keys
OPENROUTER_API_KEY=""
TWILIO_ACCOUNT_SID=""
TWILIO_AUTH_TOKEN=""

# Supabase (cloud or docker compose PostgREST)
# For docker compose: SUPABASE_URL=http://rest:3000
SUPABASE_URL=""
SUPABASE_KEY=""

# Docker compose PostgREST JWT (must match docker-compose.yml PGRST_JWT_SECRET)
SUPABASE_ANON_KEY="eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJyb2xlIjoiYW5vbiJ9.ZopqoUt20nEV9cklpv9e3yCwind2FvMNT8CDGaHU-BI"

# Redis (set by docker compose, or point to external instance)
REDIS_URL=""

# Web Push Notifications (VAPID keys)
# Generate with: python -c "from py_vapid import Vapid; v=Vapid(); v.generate_keys(); print('PRIVATE:', v.private_pem()); print('PUBLIC:', v.public_key)"
VAPID_PRIVATE_KEY=""
VAPID_PUBLIC_KEY=""
VAPID_CONTACT="mailto:help@dater.world"

# Optional: Paystack billing
PAYSTACK_SECRET_KEY=""

# Optional Twilio Testing
TWILIO_PHONE_NUMBER=""
